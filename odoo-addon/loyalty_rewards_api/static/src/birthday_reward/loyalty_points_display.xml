<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <!--
        Extension of point_of_sale.OrderSummary (applied after pos_loyalty's extension).
        Replaces the points display div inside the loyalty foreach:
        - Virtual card (couponId < 0) = customer not enrolled → show warning message
        - Real card (couponId > 0) = enrolled → show normal points tally
        isVirtual is set by the PosOrder.getLoyaltyPoints patch in birthday_reward.js
    -->
    <t t-name="loyalty_rewards_api.OrderSummary"
       t-inherit="point_of_sale.OrderSummary"
       t-inherit-mode="extension">
        <xpath expr="//t[@t-as='_loyaltyStat']/div" position="replace">
            <t t-if="_loyaltyStat.isVirtual">
                <div class="d-flex justify-content-between px-2 py-2 bg-view">
                    <div t-esc="_loyaltyStat.points.name" class="loyalty-points-title fs-4 fw-bolder"/>
                    <div class="fw-bold fst-italic text-warning">No inscrito en el programa</div>
                </div>
            </t>
            <t t-else="">
                <div t-if="_loyaltyStat.points.won || _loyaltyStat.points.spent"
                     class="d-flex justify-content-between px-2 py-2 bg-view">
                    <div t-esc="_loyaltyStat.points.name" class="loyalty-points-title fs-4 fw-bolder"/>
                    <div class="d-flex gap-1 fw-bold">
                        <div t-if="_loyaltyStat.points.balance" class="loyalty-points loyalty-points-balance">
                            <span class="value"><t t-esc="_loyaltyStat.points.balance"/></span>
                        </div>
                        <div t-if="_loyaltyStat.points.won" class="loyalty-points loyalty-points-won">
                            <span class="value text-success">+<t t-esc="_loyaltyStat.points.won"/></span>
                        </div>
                        <div t-if="_loyaltyStat.points.spent" class="loyalty-points loyalty-points-spent">
                            <span class="value text-danger">-<t t-esc="_loyaltyStat.points.spent"/></span>
                        </div>
                        =
                        <div class="loyalty-points loyalty-points-totaltext-end fw-bolder">
                            <span class="value"><t t-esc="_loyaltyStat.points.total"/></span>
                        </div>
                    </div>
                </div>
            </t>
        </xpath>
    </t>
</templates>
